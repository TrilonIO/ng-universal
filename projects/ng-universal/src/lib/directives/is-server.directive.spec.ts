import { IsServerDirective } from './is-server.directive';

describe('IsServerDirective', () => {
  it('should create an instance', () => {
    // const directive = new IsServerDirective();
    // expect(directive).toBeTruthy();
  });
});
