import { IsBrowserDirective } from './is-browser.directive';

describe('IsBrowserDirective', () => {
  it('should create an instance', () => {
    // const directive = new IsBrowserDirective();
    // expect(directive).toBeTruthy();
  });
});
